# requirements.txt (prod)
# SIP/VoIP
PySIPio
pysip     # Alternative plus complète pour SIP/RTP
sounddevice>=0.4.6  # Capture audio (si nécessaire)
scipy>=1.10.0       # Traitement audio (WAV, etc.)
# Utilitaires
uuid>=1.30          # Génération d'identifiants uniques
requests>=2.28      # Pour requêtes HTTP (si intégration API)
aiosip==0.2.0
fastapi==0.115.12
uvicorn[standard]==0.30.6
websockets==13.1
python-dotenv==1.0.1
prometheus_client==0.20.0
# dev-requirements.txt
pytest==8.3.5
pytest-asyncio==0.23.5

